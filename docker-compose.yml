services:
  app:
    build: .
    ports:
      - "7860:7860"
    env_file: backend/.env
    environment:
      - DEMO_MODE=false
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
